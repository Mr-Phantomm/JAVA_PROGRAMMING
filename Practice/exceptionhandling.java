// Create a custom exception class called InvalidAgeException that extends Exception. Then, write a method validateAge(int age) that throws this exception if the age is less than 0 or greater than 150. In the main method, demonstrate catching and handling this exception.

public class exceptionhandling {
    
}
